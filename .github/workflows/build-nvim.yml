name: Build Neovim & Neovim-Qt (CentOS 7 Fix)
on:
  workflow_dispatch:

jobs:
  build-and-package:
    runs-on: ubuntu-latest
    container:
      image: centos:7
    steps:
      - name: Fix Repositories (Vault Migration)
        run: |
          # 1. 禁用已失效的 mirrorlist 并强制指向 vault 存档服务器
          sed -i 's|^mirrorlist=|#mirrorlist=|g' /etc/yum.repos.d/CentOS-*.repo
          sed -i 's|^#baseurl=http://mirror.centos.org|baseurl=http://vault.centos.org|g' /etc/yum.repos.d/CentOS-*.repo
          
          # 2. 安装 SCL 和 EPEL (这一步会生成新的 .repo 文件)
          yum install -y centos-release-scl epel-release
          
          # 3. 【核心修正】对所有新生成的 Repo（包含 SCL 和 EPEL）再次执行 Vault 迁移
          find /etc/yum.repos.d/ -name "*.repo" -exec sed -i 's|^mirrorlist=|#mirrorlist=|g' {} +
          find /etc/yum.repos.d/ -name "*.repo" -exec sed -i 's|^#baseurl=http://mirror.centos.org|baseurl=http://vault.centos.org|g' {} +
          find /etc/yum.repos.d/ -name "*.repo" -exec sed -i 's|^baseurl=http://mirror.centos.org|baseurl=http://vault.centos.org|g' {} +
          
          # 4. 验证并安装所需的工具链
          yum install -y --nogpgcheck devtoolset-11-gcc devtoolset-11-gcc-c++ \
                         make git patch unzip gettext-devel \
                         qt5-qtbase-devel qt5-qtsvg-devel \
                         wget curl tar gzip

      - name: Install CMake 3.26
        run: |
          # Neovim 0.11 需要高版本 CMake，CentOS 自带版本太低
          wget https://github.com/Kitware/CMake/releases/download/v3.26.4/cmake-3.26.4-linux-x86_64.sh
          sh cmake-3.26.4-linux-x86_64.sh --skip-license --prefix=/usr/local

      - name: Build Neovim Core
        shell: bash
        run: |
          source /opt/rh/devtoolset-11/enable
          # 0.11 处于 master 分支，这里克隆最新的代码
          git clone --depth 1 https://github.com/neovim/neovim.git nvim-src
          
          cd nvim-src
          # USE_BUNDLED=ON 会自动下载并静态编译依赖，避免链接系统老旧库导致的符号错误
          make CMAKE_BUILD_TYPE=Release \
               CMAKE_EXTRA_FLAGS="-DCMAKE_INSTALL_PREFIX=$GITHUB_WORKSPACE/nvim-dist -DUSE_BUNDLED=ON" \
               LDFLAGS="-static-libstdc++ -static-libgcc"
          make install

      - name: Build Neovim-Qt
        shell: bash
        run: |
          source /opt/rh/devtoolset-11/enable
          git clone --depth 1 https://github.com/equalsraf/neovim-qt.git
          cd neovim-qt
          mkdir build && cd build
          /usr/local/bin/cmake -DCMAKE_BUILD_TYPE=Release \
                 -DQT_QMAKE_EXECUTABLE=/usr/bin/qmake-qt5 \
                 -DCMAKE_INSTALL_PREFIX=$GITHUB_WORKSPACE/nvim-dist \
                 -DCMAKE_EXE_LINKER_FLAGS="-static-libstdc++ -static-libgcc" ..
          make -j$(nproc)
          make install

      - name: Create Tarball
        run: |
          cd $GITHUB_WORKSPACE
          # 创建包含核心、运行时文件和 GUI 的压缩包
          tar -czvf nvim-0.11-centos7-x64.tar.gz -C nvim-dist .

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: nvim-centos7-package
          path: nvim-0.11-centos7-x64.tar.gz
